{
  "name": "@testcrate/core",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./test": "./test/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "dev:test": "vitest watch",
    "test": "vitest run",
    "lint": "eslint . --fix",
    "lint:ci": "eslint .",
    "build": "tsc --noEmit"
  },
  "files": [
    "src"
  ],
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@testcrate/eslint-config-cli": "workspace:*",
    "@testcrate/prettier-config-cli": "workspace:*",
    "@testcrate/typescript-config-cli": "workspace:*",
    "vitest": "^3.1.3"
  },
  "prettier": "@testcrate/prettier-config-cli"
}
